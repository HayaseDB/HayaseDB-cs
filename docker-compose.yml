services:
  web:
    container_name: HayaseDB-cs
    image: nginx:latest
    ports:
      - ${PORT}:80
    pull_policy: build
    volumes:
      - ${PWD}/nginx.conf:/etc/nginx/conf.d/default.conf
      - ${PWD}/src:/usr/share/nginx/html/
